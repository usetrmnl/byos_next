// Shared constants for recipe rendering
// This file must not import any server-only dependencies (next/og, sharp, etc.)
// so it can be safely imported in client components.

export const DEFAULT_IMAGE_WIDTH = 800;
export const DEFAULT_IMAGE_HEIGHT = 480;
